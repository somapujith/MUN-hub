{
  "name": "mun-hub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:ci": "vitest run --no-file-parallelism",
    "test:watch": "vitest",
    "test:e2e": "playwright test -c E2E/playwright.config.ts",
    "test:e2e:ui": "playwright test -c E2E/playwright.config.ts --ui",
    "test:e2e:report": "playwright show-report E2E/playwright-report",
    "typecheck": "npm run typecheck:root && npm run typecheck:e2e && npm --prefix server run typecheck && npm --prefix web run typecheck",
    "typecheck:root": "tsc --noEmit -p tsconfig.json",
    "typecheck:e2e": "tsc --noEmit -p E2E/tsconfig.json",
    "lint": "npm --prefix web run lint && npm run lint:shared",
    "lint:shared": "node web/node_modules/oxlint/bin/oxlint lib server E2E",
    "check": "npm run typecheck && npm run lint",
    "db:up": "docker compose up -d",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx lib/db/migrate.ts",
    "db:migrate:local": "node scripts/with-local-db.mjs npx tsx lib/db/migrate.ts",
    "db:push": "drizzle-kit push",
    "db:seed": "tsx lib/db/seed.ts",
    "db:seed:local": "node scripts/with-local-db.mjs npx tsx lib/db/seed.ts",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "@hono/zod-validator": "^0.9.1",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.7",
    "postgres": "^3.4.9",
    "zeptomail": "^8.0.1",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^20",
    "dotenv": "^17.4.2",
    "drizzle-kit": "^0.31.10",
    "eslint": "^9",
    "tsx": "^4.23.13",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
