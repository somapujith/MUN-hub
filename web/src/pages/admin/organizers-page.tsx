import { AdminPageFrame } from "@/components/admin/admin-page-frame";

export function AdminOrganizersPage() {
  return (
    <AdminPageFrame title="Organizers" description="Organizer accounts and application history (mock shell).">
      <div className="rounded-md border border-dashed border-border bg-card p-lg text-body-md text-muted-foreground">
        Organizer directory placeholder — wire to admin search in Phase 6.3.
      </div>
    </AdminPageFrame>
  );
}
