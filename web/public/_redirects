# Redirects for Cloudflare Workers Static Assets (munhub-web). Keep in sync
# with web/vercel.json.
#
# /sitemap.xml is NOT listed here on purpose: it used to be a 302 to the API,
# which search engines are allowed to skip. It's now proxied with a real 200
# by web/src/server/social-preview-worker.ts (run_worker_first intercepts it
# before this file is ever consulted).
