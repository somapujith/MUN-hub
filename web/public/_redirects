# Redirects for Cloudflare Workers Static Assets (munhub-web). Keep in sync
# with web/vercel.json. The sitemap is generated by the API; without this the
# SPA fallback would answer /sitemap.xml with index.html.
/sitemap.xml https://api.munhub.in/api/v1/sitemap.xml 302
