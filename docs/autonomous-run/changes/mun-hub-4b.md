# mun-hub-4b: email/notifications lane

Owns `lib/notifications/**` and email-verification code. This file is the running change log for the autonomous run.

## Before the run (already on main)

- ZeptoMail adapter + selector (bca0883), landed real production email delivery — confirmed with a live send against `api.munhub.in` (204, 554ms, no delivery-failure log).
- `NotificationPayload.html` + branded OTP email template (a8bea5e), consumed by mun-hub-f1's organizer OTP login (147fc18).

## Log

| Time | Commit | Change |
|---|---|---|
| T+0 | 90125d3 | resolve-recipients.ts: buildPublicMunUrl reads APP_URL via getRuntimeEnv, not process.env. Added APP_URL to .env/.env.example/.env.test/.dev.vars/wrangler.jsonc. Flagged server/routes/sitemap.ts's same-bug NEXT_PUBLIC_APP_URL to mun-hub-62 (not my lane). |
| T+0 | 21b6c9c | Wired PUBLISHING, RESUBMISSION (vs NEW_SUBMISSION), UNDER_REVIEW, READY_FOR_SUBMISSION pipeline events (go-live.ts, organizer-confirmation.ts, module-completion.ts) + 5 new tests in go-live-notifications.test.ts. ONBOARDING_STARTED still pending — its trigger is in lib/actions/admin-review.ts, asked mun-hub-62 who owns that file. |
| T+0 | 5ba5d94 | Gate-1 decision emails: new lib/notifications/organizer-application-events.ts (separate OrganizerApplicationEvent union, not PipelineEvent variants — Gate-1/Gate-2 collision avoidance) + wired into reviewMunApplication (admin-review.ts), which also now fires ONBOARDING_STARTED on APPROVED. Coordinated turns on admin-review.ts with mun-hub-f1. All 5 originally-listed pipeline events now wired. |
| T+0 | 5559c00 | Delegate emails: notifyRegistrationConfirmed/notifyPaymentFailed/notifySeatHoldExpired (registration-events.ts), notifyWelcome (welcome-email.ts), notifySupportReply (support-reply-email.ts, wired directly into support.ts#sendMessage since I own that file). All respect users.emailNotificationsEnabled via new email-preference.ts. Sent exact signatures to mun-hub-62 for their payments/lifecycle hook wiring. |
| T+0 | ffb6342 | Security hardening (audit finding): getNotificationsAdapter() throws in production without ZEPTOMAIL_TOKEN instead of silently logging OTP/reset links to console. Dev/test EMAIL_OUTBOX_FILE feature for mun-hub-84's E2E coverage (JSON lines: to/subject/text/html/sentAt), never active in production. |
| T+0 | 97e0ce7 | Email verification: migration 0033 (email_verification_tokens, users.email_verified_at, backfilled), lib/actions/email-verification.ts (send/resend/verify/isEmailVerified/assertEmailVerifiedIfRequired), POST /verify-email + /verify-email/resend routes, /verify-email web page + resend form. assertEmailVerifiedIfRequired NOT wired into registration.ts by me — mun-hub-62 adds that call at merge. All lane tasks now complete: 5/5 pipeline events, Gate-1 emails, delegate emails, production hardening, email verification. |
| T+1 | 270680b | New tasks from mun-hub-62: SLA job runSlaNotifications (lib/notifications/sla-job.ts) — idempotent via the existing mun_submissions.slaState column as a watermark, no new migration. Landed inside a merge commit (270680b, "Merge branch 'worktree-wf_a8625366-3be-3'") — my `git add` raced an in-progress merge's conflict resolution; confirmed with mun-hub-62 the content is intact and unchanged (0-line diff against my working tree). |
| T+1 | e5c6471 | Conference-cancelled emails: lib/notifications/conference-events.ts#notifyConferenceCancelled (every CONFIRMED delegate + organizer-if-admin-cancelled, reason from verification_logs, no-refund-wording, /legal/refunds link). lib/lifecycle/lifecycle-events.ts's stub now re-exports it — that file is mine per mun-hub-62. Staff 2FA (migration slot 0034 reserved) on hold until the sec-auth lane merges — waiting on mun-hub-62's go-ahead. |
| T+1 | 48500ed | Delegate reminders: lib/notifications/reminder-job.ts#runConferenceReminders(now) — emails CONFIRMED delegates ~24h before their mun starts (dates/venue/pass link/mun link). Idempotent for a 5-min cron via a fixed-point half-open window (startDate-24h in (now-5min, now]), no migration. Deliberately skips emailNotificationsEnabled (documented judgment call — operational, not discretionary). 5/5 tests. First commit attempt hit `fatal: cannot do a partial commit during a merge` (devops-lane merge in progress); safely unstaged and retried once the merge cleared — no collision this time. |
| T+1 | da92d65 | Organizer alerts: lib/notifications/organizer-digest-job.ts#runOrganizerDigest(now) — once-daily digest (09:00-09:05 IST window) to each organizer with a REGISTRATION_OPEN mun: new confirmed registrations in the last 24h, seats-left per active pass, SOLD OUT callout at capacity. Multiple open muns per organizer grouped into one email. "New confirmed" approximates confirmedAt via registrations.updatedAt (documented). 4/4 tests. Both cron-callable exports (runConferenceReminders, runOrganizerDigest) now ready for mun-hub-62's scheduler alongside runSlaNotifications. |
| T+2 | 35cd296 | Deep-linked support reply emails: notifySupportReply now links to `/dashboard/support?ticket=<id>` / `/organizer/support?ticket=<id>` instead of the general inbox, now that the support lane's inboxes read that query param. Requested by mun-hub-62 after the support lane merged. |
| T+2 | 900f476, e6a0db9 | Staff TOTP two-factor auth, unblocked once mun-hub-62 confirmed the sec-auth lane merged (1fabca7) — migration 0034 (reserved slot): user_mfa/mfa_recovery_codes/mfa_pending_challenges. New lib/auth/totp.ts (RFC 6238/4226 over node:crypto, verified against the published RFC test vectors). lib/crypto/field-encryption.ts generalized to take an optional key-domain override (default PAYMENT_FIELD_KEY unchanged for every existing caller) so TOTP secrets encrypt under their own TOTP_FIELD_KEY — first real caller of decryptField, since a TOTP secret genuinely needs decrypting on every check (unlike payment fields, which stay write-only). New lib/actions/staff-mfa.ts: enrollment (begin/confirm, 10 scrypt-hashed recovery codes), the sign-in challenge (row-locked attempt counter mirroring organizer-otp.ts), SUPER_ADMIN-only reset. lib/actions/auth.ts#signIn now returns a SignInResult union — SIGNED_IN unchanged for everyone except a staff account with *confirmed* MFA, which gets MFA_REQUIRED (hashed pendingToken) instead. server: POST /auth/session/mfa, GET/POST /auth/mfa/{status,setup,confirm} (requireAuth only, so an enforced-but-unenrolled account can still reach setup), POST /admin/staff/:id/mfa/reset. server/middleware/require-role.ts blocks staff-role routes for unconfirmed MFA only when REQUIRE_STAFF_2FA=true (env check first — zero behavior change, including the seeded admin's E2E login, whenever it's unset). Rate limiters RL_MFA_VERIFY_IP/TOKEN (namespace ids 1015/1016). 107 tests across lib+integration, tsc clean. `.env.example`'s TOTP_FIELD_KEY/REQUIRE_STAFF_2FA doc lines are written in the working tree but not committed by me — that file had another session's concurrent restructuring mixed in, so I left it for whoever commits the rest of it rather than committing over their uncommitted work. |
| T+2 | 44825cb | Self-service MFA disable/regenerate + clean unavailable error, per mun-hub-62's follow-up asks: regenerateMfaRecoveryCodes (requires a fresh TOTP code, not a recovery code) and disableMfa (TOTP or recovery code), POST /auth/mfa/recovery-codes and /auth/mfa/disable. beginMfaEnrollment now checks TOTP_FIELD_KEY up front and throws MFA_ERRORS.unavailable (503 MFA_UNAVAILABLE) instead of a raw 500. The matching error.ts mapping is written but not committed — same shared-file situation as .env.example. 24/24 tests. |
| T+2 | 623ae38 | Staff MFA web UI: login-form.tsx grows a code step (TOTP or recovery code) when POST /auth/session returns MFA_REQUIRED; new /admin/security page (status, setup with manual key + otpauth:// link — no QR image, no QR lib exists in web/node_modules and I was asked not to add one — confirm, 10 recovery codes shown once with copy/download, regenerate, disable), linked from the admin nav and the staff user-menu; SUPER_ADMIN "Reset 2FA" button on the admin Staff page. tsc -b clean, oxlint clean. |
| T+2 | 4d3d86e | Fixed a real bug found by mun-hub-84's E2E (not mine to have caught, but in my file): lib/notifications/registration-events.ts's formatMoney divided by 100 as if amounts were paise; they're whole rupees everywhere else in this codebase (web/src/components/shared/currency.tsx's formatPrice runs Intl.NumberFormat directly on the raw value). A ₹1,499 registration emailed as "INR 14.99". Fixed to match formatPrice exactly — en-IN grouping, ₹ symbol. Checked every other notifications file for the same mistake (none had it). 7/7 tests including a regression test and one for lakh-grouping. Told 84 to flip their test.fail marker. |
| T+2 | c517696 | Admin/Ops UX pass (Phase 3, docs/autonomous-run/PLAN.md), real browser via Playwright against a system Chrome install (sandbox has no network access for Playwright's own Chromium download), own ports (API 3901, web 5901), desktop+mobile. Found and fixed a real bug myself: browser autofill bled the just-typed email into the MFA code field on the sign-in step swap, because React was reusing the same `<input>` DOM node across two early-return branches with no differentiating `key` — added one per step, confirmed fixed via the input's actual `.value`. Also fixed the two things f1 found (Gate-1 *and* Gate-2 review dialog footers cut off at 900px — same bug in both, fixed both; Applications queue MUN name not linked, now links to /admin/muns/:munId) and one I found myself (Registrations search box placeholder visibly clipped — max-w-sm was on the wrong element). Full findings, including two sent-onward structural items (Payments page has zero pagination — needs a server change, out of my authorized web-only scope; Applications/Verification/Audit have no search/filter unlike every other list page), in docs/autonomous-run/ux/admin.md. |
